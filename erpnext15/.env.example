# 使用你构建并推送的镜像
CUSTOM_IMAGE=ghcr.io/yourname/erpnext15
CUSTOM_TAG=latest-v15
PULL_POLICY=always

# 外部数据库（MariaDB/MySQL）
DB_HOST=db.example.com
DB_PORT=3306
DB_ROOT_USER=root
MYSQL_ROOT_PASSWORD=your-root-password

# 如果站点安装阶段需要初始库与用户（可选）
SITE_NAME=site1.local
ADMIN_PASSWORD=Admin@123

# 外部 Redis（cache / queue / socketio）
REDIS_CACHE=redis-cache.example.com:6379
REDIS_QUEUE=redis-queue.example.com:6379
REDIS_SOCKETIO=redis-socketio.example.com:6379
SOCKETIO_PORT=9000

# 反代端口（按你的环境修改）
HTTP_PUBLISH_PORT=8080
HTTPS_PUBLISH_PORT=443
